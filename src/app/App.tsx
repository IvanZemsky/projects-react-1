import {
   Box,
   Typography,
} from "@mui/material";


export const App = () => {
   return (
      <Box>
         <Typography variant="h1">Project management system</Typography>
      </Box>
   );
};
